# LLM 설정 — 폐쇄망 모델 서버에 맞게 수정
MODEL_NAME=Qwen3-8B
MODEL_SERVER=http://127.0.0.1:8000/v1
MODEL_API_KEY=EMPTY

# 선택 파라미터 (기본값으로 동작 — 변경 불필요 시 주석 유지)
# DIRECT_INJECT_TOKEN_LIMIT=12000
# WORKER_MAX_STEPS=10
# REFLECT_INTERVAL=3
# API_HOST=0.0.0.0
# API_PORT=8100
